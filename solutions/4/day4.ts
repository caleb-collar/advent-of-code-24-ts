import { colors, reset } from '../../util/util.ts';

const title = 'Ceres Search 🛰️';

const dayFour = (lines: string[]) => {
  console.log(`${colors[3]}${title}${reset}`);
  //Invoke solution
  //I'm going to use a graph and search nodes starting from every X node

  console.log('❄ WORD SEARCH RESULTS: ');
  console.log('❄ ');
};

export default dayFour;
